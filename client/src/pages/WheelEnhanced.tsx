import { Dices, Sparkles } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";
import { levelForXp, type AppConfig, type ProfileState, type WheelReward } from "../../../shared/study";

function weightedPick(rewards: WheelReward[]) { const total = rewards.reduce((sum, reward) => sum + Math.max(0, reward.probability), 0); if (!total) return rewards[Math.floor(Math.random() * rewards.length)]; let cursor = Math.random() * total; for (const reward of rewards) { cursor -= Math.max(0, reward.probability); if (cursor <= 0) return reward; } return rewards.at(-1)!; }
export default function WheelEnhanced({ profile, config, onProfile }: { profile: ProfileState; config: AppConfig; onProfile: (profile: ProfileState, message?: string) => void }) { const [spinning, setSpinning] = useState(false); const [result, setResult] = useState<WheelReward | null>(null); const total = config.wheelRewards.reduce((sum, reward) => sum + reward.probability, 0); const spin = () => { if (!profile.wheelTickets) return toast.error("Bạn chưa có vé quay."); if (!config.wheelRewards.length) return toast.error("Admin/Founder chưa cấu hình vòng quay."); setSpinning(true); const reward = weightedPick(config.wheelRewards); window.setTimeout(() => { const next = { ...profile, wheelTickets: profile.wheelTickets - 1, xp: profile.xp + (reward.kind === "xp" ? reward.value : 0), fragments: reward.kind === "fragment" ? { ...profile.fragments, general: (profile.fragments.general ?? 0) + reward.value } : profile.fragments }; next.level = levelForXp(next.xp); setResult(reward); setSpinning(false); onProfile(next, `Nhận được ${reward.label}.`); }, 1150); };
 return <><div className="mb-7 flex flex-col gap-3 md:flex-row md:items-end md:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-cyan-600 dark:text-cyan-300">Vòng quay tri thức</p><h1 className="mt-2 font-display text-3xl font-bold text-slate-950 dark:text-white">Một phần thưởng cho nỗ lực</h1><p className="mt-2 text-sm text-slate-500">Kết quả được chọn theo tỷ lệ phần trăm mà Admin/Founder cấu hình.</p></div><span className="rounded-xl bg-violet-50 px-4 py-2 text-sm font-bold text-violet-700">{profile.wheelTickets} vé quay</span></div><div className="grid gap-5 lg:grid-cols-2"><section className="grid min-h-96 place-items-center rounded-[2rem] bg-gradient-to-br from-[#11144e] via-violet-800 to-blue-700 p-8 text-center text-white"><div className={spinning ? "animate-spin" : ""}><div className="grid h-64 w-64 place-items-center rounded-full border-[14px] border-white/80 bg-[conic-gradient(#22d3ee_0deg_70deg,#7c3aed_70deg_150deg,#fbbf24_150deg_220deg,#34d399_220deg_300deg,#60a5fa_300deg_360deg)]"><span className="grid h-32 w-32 place-items-center rounded-full bg-slate-950 font-display font-bold">TRI THỨC</span></div></div><button onClick={spin} disabled={spinning} className="mt-8 rounded-xl bg-cyan-300 px-6 py-3 text-sm font-bold text-slate-950 disabled:opacity-50">{spinning ? "Đang quay…" : "Quay ngay"}</button>{result && <p className="mt-4 inline-flex items-center gap-2 text-sm font-bold text-cyan-200"><Sparkles className="h-4 w-4" />{result.label}</p>}</section><section className="panel p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-slate-400">Bảng phần thưởng</p><h2 className="mt-2 font-display text-xl font-bold">Tổng tỷ lệ: {total}%</h2></div><Dices className="h-6 w-6 text-violet-500" /></div>{config.wheelRewards.length ? <div className="mt-5 grid gap-3 sm:grid-cols-2">{config.wheelRewards.map((reward) => <div key={reward.id} className="rounded-2xl border border-slate-100 p-4 dark:border-white/10"><span className="block h-2 w-10 rounded-full" style={{ background: reward.color }} /><b className="mt-3 block text-sm">{reward.label}</b><small className="text-slate-500">{reward.kind} · {reward.value} · {reward.probability}%</small></div>)}</div> : <p className="mt-5 rounded-xl bg-slate-50 p-4 text-sm text-slate-500 dark:bg-white/5">Chưa có phần thưởng được cấu hình.</p>}</section></div></>; }
